---
title: "Midterm Take Home Portion"
author: "Jenn Lewis, Tamara Niella"
date: "4/27/2019"
output: html_document
---

```{r setup}
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(cache = TRUE)
#install.packages("glue")
library(glue)
library(rio)
library(purrr)

```

```{r Part A 1}
download_file <- function(year) {
    link <- glue::glue("https://www.oregon.gov/ode/educator-resources/assessment/TestResults20{year}/pagr_schools_ela_raceethnicity_{year-1}{year}.xlsx")
    rio::import(link, setclass = "tibble", na = c("-", "--", "*"))
}

data <- map_df(15:18, download_file, .id = "year_link")

#Clean Data Renamed Data to not override original data set

link_filtered <- link %>%
  filter(student_group == "White" | student_group == "Hispanic/Latino") %>%
  select() %>%
  filter(n != "NA")


```


```{r cars}
summary(cars)
```



```{r pressure, echo=FALSE}
plot(pressure)
```


